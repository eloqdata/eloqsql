platform: linux
image_resource:
  type: registry-image
inputs:
  - name: eloqsql_src
outputs:
  - name: the-output
run:
  path: /bin/bash
  args:
    - eloqsql_src/concourse/scripts/build_tarball.bash
