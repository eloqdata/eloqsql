// need codelldb vscode extension
{
    "configurations": [
        {
            "name": "launch mysqld",
            "type": "lldb",
            "request": "launch",
            "program": "/path/to/installed/mysqld",
            "args": [
                "--defaults-file=/path/to/mycnf"
            ]
        }
    ]
}